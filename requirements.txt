# --- SafeDeal Town : 실행에 필요한 패키지 ---
# 설치:  pip install -r requirements.txt
# (가상환경은 따로 안 만들어도 됨. 만들고 싶으면: python -m venv .venv)

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
httpx==0.28.1
python-dotenv==1.0.1
